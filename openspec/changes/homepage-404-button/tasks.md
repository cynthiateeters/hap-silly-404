<!-- Generated 2026-04-29 by Copilot agent for openspec workflow -->

# Tasks: homepage-404-button

1. Add a button markup to `index.html` linking to `/nonexistent-page` with an `aria-label`.
2. Style the button minimally in `css/style.css` or `css/404.css` to match existing buttons.
3. Run `npm run check` and `netlify dev` locally to validate the flow.
4. Commit and push the change, then archive the openspec change.
