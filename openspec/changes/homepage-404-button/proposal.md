<!-- Generated 2026-04-29 by Copilot agent for openspec workflow -->

# Proposal: homepage-404-button

## Why

This is the required practice run for Part 4A of the Silly 404 module. Adding a single homepage button that links to a known-bad URL gives everyone the same small, testable feature to exercise the openspec→agent→implement workflow.

## What Changes

- Add a visible button to `index.html` that points to `/nonexistent-page` (or similar) so the 404 page is triggered when clicked.
- Wire any minimal accessible markup and styles; no JavaScript required.

## Deliverables

- `openspec/changes/homepage-404-button/` artifacts (proposal, design, tasks, spec).
- A UI update to `index.html` and a small CSS tweak if needed.
